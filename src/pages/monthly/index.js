import * as React from 'react';
// import {useDispatch, useSelector} from 'react-redux'
// import {decrement, getImage} from '../../redux/daily/dailyReducer'

export default function Monthly() {
    // const count = useSelector((state) => state.dailyReducer.value1)
    // const type = useSelector((state) => state.dailyReducer.type)
    // const dispatch = useDispatch()

    // console.log(type)
    return (
        <div>
            {/*<div>*/}
            {/*    <button aria-label="Increment value" onClick={() => dispatch(getImage())}>*/}
            {/*        Increment*/}
            {/*    </button>*/}
            {/*    <span>{count}</span>*/}
            {/*    <span>{type}</span>*/}
            {/*    <button aria-label="Decrement value" onClick={() => dispatch(decrement())}>*/}
            {/*        Decrement*/}
            {/*    </button>*/}
            {/*</div>*/}
        </div>
    )
}